import React from "react";
import Navbar from "react-bootstrap/Navbar";

import { Container } from "./styles";

const Header: React.FC = () => {
  return <Container>Começando no REACT</Container>;
};

export default Header;
